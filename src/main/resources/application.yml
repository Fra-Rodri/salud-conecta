spring:
  application:
    name: SaludConecta

# Datos de conexión a la bbdd
  datasource:
    url: jdbc:postgresql://localhost:5432/saludconectadb
    username: frarod
    password: frarod
    driver-class-name: org.postgresql.Driver

  jooq:
    sql-dialect: POSTGRES

# Usuario para el login
  security:
    user:
      name: user
      password: user

# Configuración del SMTP para el envío de correos
  mail:
    host: smtp.gmail.com
    port: 587
    username: rodriguezxf@gmail.com
    password: 
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        debug: true

# Trazas para el envío de correos
logging:
  level:
    org.springframework.mail: DEBUG
    jakarta.mail: DEBUG